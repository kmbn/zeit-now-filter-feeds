{
  "version": 2,
  "name": "filter-feeds",
  "builds": [
    {
      "src": "index.py",
      "use": "@now/python",
      "config": { "maxLambdaSize": "8mb" }
    }
  ],
  "routes": [{ "src": "/(?<feed>[^/]+)", "dest": "/index.py?feed=$feed" }]
}
